<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>System Status Dashboard - {{ ansible_hostname }}</title>
  <style>
    body { font-family: Verdana, sans-serif; background: #eef; margin: 0; padding: 0; }
    header { background: #005a9c; color: #fff; padding: 20px; text-align: center; }
    .container { width: 80%; margin: 20px auto; background: #fff; padding: 20px; border-radius: 5px; }
    h2 { color: #005a9c; }
    p { font-size: 16px; }
  </style>
</head>
<body>
  <header>
    <h1>System Status Dashboard</h1>
  </header>
  <div class="container">
    <section>
      <h2>Betriebssystem</h2>
      <p>{{ ansible_distribution }} {{ ansible_distribution_version }}</p>
    </section>
    <section>
      <h2>CPU</h2>
      <p>{{ cpu_info }}</p>
    </section>
    <section>
      <h2>Festplattenspeicher (C:)</h2>
      <p>Gesamter Speicherplatz: {{ disk_size_gb }} GB</p>
      <p>Freier Speicherplatz: {{ disk_free_gb }} GB</p>
    </section>
    <section>
      <h2>Aktuelles Datum/Uptime</h2>
      <p>{{ '%A, %d. %B %Y, %H:%M:%S' | strftime(epoch_time | int + 3600) }}</p>
    </section>
  </div>
</body>
</html>